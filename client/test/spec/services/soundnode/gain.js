'use strict';

describe('Service: SoundNode/Gain', function () {

  // load the service's module
  beforeEach(module('webClientSideApp'));

  // instantiate service
  var SoundNode/Gain;
  beforeEach(inject(function (_SoundNode/Gain_) {
    SoundNode/Gain = _SoundNode/Gain_;
  }));

  it('should do something', function () {
    expect(!!SoundNode/Gain).toBe(true);
  });

});
