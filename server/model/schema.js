// var mongoose = require('mongoose');
// var Schema = mongoose.Schema;

// var dbName = 'dbsound';
// var port = '27017';
// mongoose.connect('mongodb://localhost:' + port + '/' + dbName);

// var Connection = new Schema({
//     name: String,
//     extension: String
// }, {collection: 'sound', versionKey: false} );

// module.exports = mongoose.model('Conn', Connection);